from pathlib import Path

from hotsearch import PROJECT_ROOT

DATA_DIR = PROJECT_ROOT / "data" / "cache"
CACHE_DIR = DATA_DIR
VOICES_CONFIG = PROJECT_ROOT / "src" / "hotsearch" / "config" / "voices.json"
