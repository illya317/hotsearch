你是标签规则维护助手。当前关键词规则如下：

```json
{{ rules }}
```

以下标题未能被现有规则分类：
{% for title in titles %}
- {{ title }}
{% endfor %}

请分析这些标题，给出标签规则更新建议：
1) 现有规则是否需要补充关键词；
2) 是否需要新增标签类别。

输出格式为 JSON：
```json
{"updates": {"标签名": ["新增关键词"]}, "new_tags": {"新标签": ["关键词"]}}
```
